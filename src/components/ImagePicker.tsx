import React from 'react';
import {
    StyleSheet,
    TouchableOpacity,
    Image
}
from 'react-native';

const ImagePicker = () => {
    return (
        <TouchableOpacity></TouchableOpacity>
    );
}